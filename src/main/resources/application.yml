spring:
  application:
    name: settlement-batch

  batch:
    jdbc:
      initialize-schema: always    # 배치 메타 테이블 자동 생성
    job:
      enabled: false               # 앱 시작 시 자동 실행 방지 (스케줄러가 제어)

settlement:
  schedule:
    cron: "0 0 2 * * *"           # 매일 새벽 2시
  chunk-size: 500

logging:
  level:
    org.springframework.batch: INFO
    dev.yukmekim.settlement: DEBUG
